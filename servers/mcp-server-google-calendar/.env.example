# Google Calendar MCP Server Configuration
# =========================================

# Server port (default: 3000)
PORT=3000

# Log level: debug, info, warn, error
LOG_LEVEL=info

# Note: Google Calendar OAuth access token is NOT configured here.
# It must be passed dynamically via HTTP headers:
#   - x-calendar-token: ya29.****
#   - Authorization: Bearer ya29.****
