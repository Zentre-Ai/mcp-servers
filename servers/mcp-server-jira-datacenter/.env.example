# Jira MCP Server Configuration
# ==============================

# Server port (default: 3000)
PORT=3000

# Log level: debug, info, warn, error
LOG_LEVEL=info

# Note: Jira credentials are NOT configured here.
# They must be passed dynamically via HTTP headers:
#
# Option 1: Basic Auth (API Token)
#   x-jira-host: your-domain.atlassian.net
#   x-jira-email: your-email@example.com
#   x-jira-token: your-api-token
#
# Option 2: OAuth 2.0 Bearer Token
#   x-jira-host: your-domain.atlassian.net
#   Authorization: Bearer your-oauth-token
