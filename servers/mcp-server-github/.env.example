# Server Configuration
PORT=3000
LOG_LEVEL=info

# GitHub credentials are passed via HTTP headers per-request:
# - x-github-token: GitHub Personal Access Token or OAuth token
# - Authorization: Bearer <token> (alternative)
