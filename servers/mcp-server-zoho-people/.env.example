# Zoho People MCP Server Configuration
# ======================================

# Server port (default: 3000)
PORT=3000

# Log level: debug, info, warn, error
LOG_LEVEL=info

# OAuth 2.0 Configuration
# =======================
# These are required for the OAuth flow endpoints (/oauth/*)
# Create an OAuth 2.0 app at: https://api-console.zoho.com/

# Your Zoho OAuth 2.0 app client ID
ZOHO_CLIENT_ID=your-client-id

# Your Zoho OAuth 2.0 app client secret (for US datacenter)
ZOHO_CLIENT_SECRET=your-client-secret

# DC-specific client secrets (optional)
# Zoho may require separate client secrets per datacenter
# If not set, ZOHO_CLIENT_SECRET will be used for all DCs
# ZOHO_CLIENT_SECRET_EU=your-eu-client-secret
# ZOHO_CLIENT_SECRET_IN=your-in-client-secret
# ZOHO_CLIENT_SECRET_AU=your-au-client-secret
# ZOHO_CLIENT_SECRET_JP=your-jp-client-secret
# ZOHO_CLIENT_SECRET_CN=your-cn-client-secret

# OAuth redirect URI (must match your app configuration)
OAUTH_REDIRECT_URI=http://localhost:3008/oauth/callback

# OAuth scopes (space-separated)
# Common scopes:
#   ZOHOPEOPLE.forms.ALL - Access all forms
#   ZOHOPEOPLE.employee.ALL - Full employee access
#   ZOHOPEOPLE.attendance.ALL - Full attendance access
#   ZOHOPEOPLE.leave.ALL - Full leave access
OAUTH_SCOPES=ZOHOPEOPLE.forms.ALL ZOHOPEOPLE.employee.ALL ZOHOPEOPLE.attendance.ALL ZOHOPEOPLE.leave.ALL

# MCP Authentication
# ==================
# Note: Zoho People credentials are NOT configured here.
# They must be passed dynamically via HTTP headers:
#
#   Authorization: Zoho-oauthtoken <access-token>
#   x-zoho-datacenter: <datacenter> (optional, default: com)
#
# Available datacenters:
#   com     - United States
#   eu      - Europe
#   in      - India
#   com.au  - Australia
#   jp      - Japan
#   com.cn  - China
