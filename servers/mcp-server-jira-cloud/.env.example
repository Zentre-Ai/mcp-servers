# Jira Cloud MCP Server Configuration
# =====================================

# Server port (default: 3000)
PORT=3000

# Log level: debug, info, warn, error
LOG_LEVEL=info

# OAuth 2.0 Configuration
# =======================
# These are required for the OAuth flow endpoints (/oauth/*)
# Create an OAuth 2.0 (3LO) app at: https://developer.atlassian.com/console/myapps/

# Your Atlassian OAuth 2.0 app client ID
ATLASSIAN_CLIENT_ID=your-client-id

# Your Atlassian OAuth 2.0 app client secret
ATLASSIAN_CLIENT_SECRET=your-client-secret

# OAuth redirect URI (must match your app configuration)
OAUTH_REDIRECT_URI=http://localhost:3000/oauth/callback

# OAuth scopes (space-separated)
# Common scopes:
#   read:jira-work - Read access to Jira issues, projects, etc.
#   write:jira-work - Write access to create/update/delete issues
#   read:jira-user - Read access to user information
#   offline_access - Get refresh tokens for long-lived access
OAUTH_SCOPES=read:jira-work write:jira-work read:jira-user offline_access

# MCP Authentication
# ==================
# Note: Jira Cloud credentials are NOT configured here.
# They must be passed dynamically via HTTP headers:
#
#   Authorization: Bearer <access-token>
#   x-jira-cloud-id: <cloud-id>
#
# Get the cloud ID from /oauth/sites after completing OAuth flow.
