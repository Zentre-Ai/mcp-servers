# Server Configuration
PORT=3010
LOG_LEVEL=info

# No Keycloak configuration needed!
# Keycloak URL and realm are automatically extracted from the access token's issuer claim.
#
# Authentication is handled via HTTP header per-request:
# - Authorization: Bearer <keycloak_access_token>
